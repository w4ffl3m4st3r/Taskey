{% extends "layouts/base.html.twig" %}

{% block title %}Tasks{% endblock %}

{% block content %}
    <h1 class="text-3xl font-bold mb-8">Tasks</h1>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        {%  for task in tasks %}
            <a href="/tasks/{{ task.id }}"
               class="block bg-white dark:bg-[#0d1117] rounded-xl shadow-sm
                hover:shadow-md dark:hover:shadow-white/10 transition p-6 border border-slate-200 dark:border-[#30363d]">

                <div class="flex justify-between items-start mb-3">
                    <h2 class="text-lg font-semibold text-slate-900 dark:text-slate-100">{{ task.title }}</h2>

                    <span class="text-cs px-2 py-1 rounded-full
                        {%  if task.status == 4 %}
                            bg-green-100 text-green-700
                        {%  elseif task.status == 3 %}
                            bg-yellow-100 text-yellow-700
                        {%  else %}
                            bg-gray-100 text-gray-600
                        {% endif %}
                    ">
                        Status {{ task.status }}
                    </span>
                </div>

                <p class="text-sm text-slate-700 dark:text-slate-300 line-clamp-2 mb-4">
                    {{ task.description }}
                </p>

                <div>
                    <div class="w-full bg-gray-200 dark:bg-[#30363d] rounded-full h-2">
                        <div class="bg-blue-500 h-2 rounded-full"
                             style="width: {{ task.progress }}%"></div>
                    </div>
                    <p class="text-xs text-slate-500 dark:text-slate-400 mt-1">{{ task.progress }}% complete</p>
                </div>

            </a>
        {% endfor %}
    </div>
{% endblock %}